<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zkit.support.server.account.access.mapper.AccessApiMapper">

    <resultMap id="ResultMap" type="org.zkit.support.server.account.access.entity.dto.AccessApi" extends="org.zkit.support.server.account.access.mapper.base.AccessApiBaseMapper.BaseResultMap">
    </resultMap>

    <sql id="Column_List">
        <include refid="org.zkit.support.server.account.access.mapper.base.AccessApiBaseMapper.Base_Column_List"/>
    </sql>

    <select id="findApisByAuthorityIds" resultMap="ResultMap">
        SELECT aa.*
        FROM access_api as aa
        JOIN access_authority_api as aaa ON aa.id = aaa.api_id
        WHERE aaa.authority_id IN
        <foreach item="authorityId" collection="authorityIds" open="(" separator="," close=")">
            #{authorityId}
        </foreach>
    </select>

</mapper>
