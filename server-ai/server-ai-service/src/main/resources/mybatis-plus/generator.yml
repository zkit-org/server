# 基础包名
basePackage: org.zkit.support.server.ai
mapperPackage: src/main/resources/mapper
# 数据源
datasource:
  url: jdbc:mysql://mysql:30310/ai
# 激活的模块
actives:
  - app
# 模块配置
modules:
  - name: app
    tables:
      - app
      - app_prompt