spring:
  application:
    name: ${APP_NAME:easy-server-authub}
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER}
        name: ${NACOS_DATA_ID:${spring.application.name}}
        file-extension: yaml
        refresh-enabled: true
      discovery:
        enabled: false