spring:
  application:
    name: ${APP_NAME:authub-server}
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER}
        name: ${NACOS_DATA_ID:${spring.application.name}}
        file-extension: yaml
        refresh-enabled: true
      discovery:
        server-addr: ${NACOS_SERVER}